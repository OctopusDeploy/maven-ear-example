This project builds a EJB jar and WAR file, and combines them into an EAR file.

Building
========

```
./mvnw clean package
```

Opening
=======

The web app responds to a GET request to `/custom-context-root/rest/members`